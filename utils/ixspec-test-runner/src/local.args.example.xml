<?xml version="1.0" encoding="UTF-8"?>
<args>
	
	<!-- #########################
         
         iXML Test Runner - Runtime Arguments
         
         If you want to override a property set in 
         local.properties.xml or project.properties.xml
         you still need to specify that property
         and its value on the command-line, at runtime
         (rather than in this file).
         
         $> ant -Dproperty=value
         
         If you don't need to override a property value, 
         you can use this file (without .example in the 
         filename) to set runtime argument values.  
         It will be loaded after local.properties.xml 
         and project.properties.xml
         
         ########################## -->
	
	<input location="${project.liana}/test/ixml/dtd_declaration.ixspec" />
	
</args>